{
  "name": "ocracy",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "start:website": "lerna run --stream --scope @ocracy/website start",
    "build:website": "lerna run --stream --scope @ocracy/website build"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
